# Database
POSTGRES_USER=
POSTGRES_PASSWORD=
# For local development outside Docker, Prisma requires DATABASE_URL.
# When using docker-compose, this is set automatically for the backend container.
DATABASE_URL=

# OpenAI
OPENAI_API_KEY=
OPENAI_MODEL=

# CORS
CORS_ORIGINS=
